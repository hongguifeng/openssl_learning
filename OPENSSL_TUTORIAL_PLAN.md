# OpenSSL 从零到嵌入式开发：教程规划（待确认）

> 当前阶段只提交课程规划，不包含正式教程章节、实验代码或生成证书。你确认结构后，再按此规划逐章实施，并对每个实验实际运行验证。

## 1. 教程定位

- **目标读者**：具备 C/C++、Linux 命令行和基本软件工程经验的嵌入式软件工程师。
- **最终能力**：能够使用 OpenSSL 3.x 的现代接口开发加密、证书、TLS 通信模块；能定位常见错误；理解库的内部架构并作出合理的移植、配置、性能和安全取舍。
- **主线**：先会用（CLI）→ 会调用（C API）→ 理解抽象层（EVP/BIO）→ 理解 TLS 和证书 → 理解内部架构 → 面向嵌入式集成和调试。
- **原则**：所有实验给出可复制命令、完整源码、预期结果和验证方式；涉及安全结论时区分“教学示例”和“生产建议”。

## 2. 工具与版本策略

实施时会先检测本机环境，然后固定可复现的基线：

- OpenSSL **3.x**：以 3.0 LTS 兼容性为基础，同时记录当前安装的较新 3.x 版本差异；不再以 ENGINE、低级 `SHA256_*` 等旧接口作为主线。
- C 编译器：GCC 或 Clang；构建：CMake + Ninja（必要时补充 Make）。
- 辅助工具：`pkg-config`、Git、Python 3、`openssl` CLI、CTest；调试阶段使用 GDB/LLDB、AddressSanitizer/UndefinedBehaviorSanitizer、Valgrind（可用时）。
- 可选环境：Docker/Podman 用于隔离不同 OpenSSL 版本；Wireshark/tshark 用于观察 TLS 握手；交叉编译工具链用于嵌入式章节。
- 每章实验尽量不依赖网络；需要网络的 TLS 实验同时提供本地自签名 CA 和本地客户端/服务端方案。

## 3. 章节规划

### 第 0 章：学习路径、密码学边界与实验环境

知识：对称/非对称/哈希/MAC/签名/KDF/随机数的职责边界；“加密”与“认证”的区别；OpenSSL 能解决什么、不能替代什么。

实验：检查编译器、CMake、OpenSSL 版本和头文件；编译一个最小 C 程序并打印版本、编译选项和运行时目录。

验证：提供环境诊断脚本和一份基线记录，后续章节引用该记录。

### 第 1 章：OpenSSL CLI 快速入门

知识：命令行工具的对象模型（摘要、随机数、密钥、CSR、证书、PKCS#12、TLS）；PEM/DER、编码与文件权限。

实验：生成随机数、摘要文件、生成 EC/RSA 密钥、查看/转换格式、创建 CSR 和自签名证书。

验证：使用独立命令交叉检查摘要、密钥公私匹配、证书主题/有效期/签名算法。

### 第 2 章：第一个 C/CMake 项目——链接、初始化与错误处理

知识：`libcrypto` 与 `libssl` 的职责；头文件、链接顺序、动态库查找；OpenSSL 3.x 初始化、清理、线程与错误队列的基本规则。

实验：建立 CMake 工程，调用版本接口、随机数接口和错误打印接口；故意制造错误并输出完整错误栈。

验证：在 GCC/Clang 下构建；用 `ldd`/等效工具确认依赖；测试失败路径不会泄漏资源。

### 第 3 章：EVP 抽象层——现代密码 API 的主线

知识：为什么优先使用 EVP；`EVP_MD`、`EVP_CIPHER`、`EVP_MAC`、`EVP_KDF`、`EVP_PKEY`、上下文对象和参数化接口；提供者（Provider）对算法发现的影响。

实验：实现 SHA-256、AES-GCM、HMAC、HKDF、RSA/ECDSA 签名验签；对比一次性接口和分段 Update/Final 接口。

验证：与 `openssl dgst`/`enc`/`mac`/`kdf` 等 CLI 结果比对；覆盖空输入、分段输入、错误密钥长度和认证标签错误。

### 第 4 章：密钥、参数与序列化格式

知识：`EVP_PKEY`、参数生成、密钥导入导出；PEM/DER、PKCS#8、SubjectPublicKeyInfo、传统 RSA 格式；密码保护和口令回调。

实验：生成/读取 RSA 与 EC 密钥；导出 PKCS#8 加密私钥；加载公钥验签；在内存 BIO 和文件 BIO 之间切换。

验证：CLI 与 C API 互操作；检查错误口令、损坏文件、权限不足等异常场景。

### 第 5 章：BIO、I/O 抽象与安全数据流

知识：BIO 链、内存/文件/socket BIO、过滤 BIO、非阻塞 I/O 的基本模型；缓冲、短读写和 `WANT_READ/WANT_WRITE`。

实验：构造内存 BIO 管道；实现一个可重入的分块加密/解密数据流；为 TLS 连接准备 socket BIO。

验证：随机分块读写、截断输入、超大输入和认证失败测试；用 Sanitizer 检查边界与生命周期。

### 第 6 章：X.509、PKI 与证书验证

知识：证书链、信任锚、SAN、KeyUsage/ExtendedKeyUsage、CRL/OCSP 概念；`X509_STORE`、验证回调、时间与主机名校验。

实验：本地搭建 Root CA → Intermediate CA → Server/Client 证书；使用 `X509_STORE_CTX` 验证链；实现主机名校验和自定义验证回调。

验证：验证成功、过期、错误用途、错误主机名、缺失中间证书、错误信任根等用例均有明确预期。

### 第 7 章：TLS 1.3 客户端与服务端

知识：TLS 握手阶段、密钥协商、证书认证、会话恢复、ALPN/SNI、密码套件和安全级别；OpenSSL 状态机与 `SSL_CTX`/`SSL` 生命周期。

实验：实现本地 TLS echo server/client；配置证书校验、SNI、ALPN、超时和非阻塞 socket；抓取并解释 TLS 1.3 握手。

验证：双向认证、错误证书、协议版本限制、ALPN 不匹配、连接中断和重连；使用 `s_client`/`s_server` 交叉测试。

### 第 8 章：OpenSSL 3.x 内部架构与接口设计

知识：`libcrypto`/`libssl` 分层；Provider/Core/Dispatch/参数传递；Fetch、属性查询、`OSSL_LIB_CTX`；默认/legacy/FIPS provider；公共 API、opaque 类型、ABI 兼容和弃用策略。

实验：列出并切换 provider；用属性查询选择算法；创建独立 `OSSL_LIB_CTX`；编写一个最小自定义 provider（先做消息摘要或简单示例算法）。

验证：追踪“应用 → EVP → Fetch → Provider”调用路径；启用库配置和调试日志，确认算法来源与属性选择符合预期。

### 第 9 章：版本演进与迁移

知识：1.0.2 → 1.1.0/1.1.1 → 3.0+ 的关键变化；opaque 结构体、自动初始化、TLS 1.3、Provider/FIPS、ENGINE 弃用；OpenSSL 与 LibreSSL/BoringSSL 的定位差异。

实验：将一段旧式低级 API 代码迁移到 EVP；编译时打开弃用警告；在两个 3.x 版本上运行兼容性测试。

验证：记录行为变化、替代 API、编译/运行时兼容边界；形成项目迁移检查清单。

### 第 10 章：嵌入式集成、裁剪与交叉编译

知识：配置选项、静态/动态链接、最小化算法集合、线程/锁、熵源、文件系统依赖、时间源、内存分配和硬件加速；FIPS 适用边界。

实验：为目标架构配置并交叉编译 OpenSSL；构建最小 TLS 客户端；替换/审计随机数和内存分配入口；测量镜像大小与握手资源。

验证：在模拟器或目标板上运行；检查随机数可用性、证书校验、断电/重启场景和资源上限。

### 第 11 章：调试、测试、性能与故障排查

知识：错误队列、调试日志、核心转储、符号化；单元/集成/互操作/模糊测试；吞吐、延迟、握手成本和内存峰值。

实验：建立 CTest 测试集；加入 Sanitizer、Valgrind 和基准测试；用故障注入定位证书、I/O、参数和 provider 错误。

验证：输出可重复测试报告；至少覆盖正常路径、异常路径、资源耗尽和并发调用。

### 第 12 章：安全工程与生产落地清单

知识：密钥生命周期、随机数、侧信道、日志脱敏、回滚/轮换、证书更新、协议降级、供应链和 CVE 响应；常见错误 API 与反模式。

实验：完成一个小型“设备安全通信模块”：配置加载、证书轮换、TLS 双向认证、重连、审计日志和故障恢复。

验证：代码审查清单、威胁模型、负面测试、静态分析和发布前检查。

### 附录

- OpenSSL CLI 速查表与 EVP API 对照表。
- 常见错误栈、错误码和排查决策树。
- PEM/DER/ASN.1/PKCS 标准之间的关系图。
- 常用配置片段、CMake 模板、交叉编译模板。
- 术语表、推荐官方文档、源码阅读路线和进一步阅读材料。

## 4. 预计仓库结构

```text
.
├── OPENSSL_TUTORIAL_PLAN.md      # 当前文件，待确认的规划
├── README.md                      # 教程入口与环境要求
├── docs/                          # 分章知识讲解
├── labs/                          # 每章独立可构建实验
├── scripts/                       # 环境检查、证书生成、结果验证
├── test/                          # CTest、互操作和负面测试
├── cmake/                         # 可复用的 CMake 配置
└── notes/                         # 版本差异、源码阅读和排错记录
```

## 5. 每章固定交付格式

1. 学习目标与先修知识。
2. 概念讲解和架构图。
3. 最小可运行实验。
4. 完整构建命令和源码说明。
5. 自动化验证脚本、预期输出和失败示例。
6. 面向嵌入式开发的注意事项。
7. 小结、练习题和下一章衔接。

## 6. 验证与质量门槛

- 实验必须在教程指定环境中实际执行，不能只给“理论上应当如此”的输出。
- 每个实验至少有一个成功用例和一个失败/边界用例。
- 证书、密钥和 TLS 实验优先使用本地临时目录，避免污染用户环境。
- 代码默认启用编译警告；关键实验加入 Sanitizer 或等效检查。
- 对 OpenSSL 版本敏感的行为明确标注版本，并尽量提供兼容写法。
- 不把教学用自签名证书、固定口令或示例密钥用于生产建议。

## 7. 建议实施顺序

先完成第 0～3 章建立可运行基础，再完成第 4～7 章形成完整“加密 + PKI + TLS”能力，随后阅读第 8～9 章理解内部实现和版本迁移，最后进行第 10～12 章的嵌入式落地、测试和安全工程。每完成一组章节都会保留可独立运行的代码和验证报告。

## 8. 需要你确认的范围

- 是否以 **C 语言 + OpenSSL 3.x + CMake** 作为主线，C++ 只做补充？
- 是否需要纳入目标 MCU/SoC、RTOS 或具体交叉编译器？如果有，请在确认时提供型号/工具链。
- 是否希望把 Provider/FIPS、自定义 provider、源码级调用链追踪作为必修内容，还是放入进阶部分？
- 是否接受使用 Docker/Podman 固定实验环境？

